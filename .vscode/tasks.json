{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Build Mod (Select Configuration)",
            "type": "process",
            "command": "dotnet",
            "args": [
                "build",
                "${workspaceFolder}/SMS/SMS.csproj",
                "/p:Configuration=${input:configuration}",
                "/p:SolutionDir=${workspaceFolder}/"
            ],
            "problemMatcher": "$msCompile",
            "group": {
                "kind": "build",
                "isDefault": true
            }
        },
        {
            "label": "Build MCM Bridge",
            "type": "process",
            "command": "dotnet",
            "args": [
                "build",
                "${workspaceFolder}/SMS.MCMBridge/SMS.MCMBridge.csproj",
                "/p:Configuration=${input:configuration}",
                "/p:SolutionDir=${workspaceFolder}/"
            ],
            "problemMatcher": "$msCompile",
            "group": "build"
        }
    ],
    "inputs": [
        {
            "id": "configuration",
            "description": "Select Build Configuration",
            "type": "pickString",
            "options": [
                "Debug",
                "Release"
            ],
            "default": "Debug"
        }
    ]
}
